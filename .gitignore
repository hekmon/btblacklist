# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# VSCode local settings
.vscode/

# Dev
btblocklist
ripe/tmp.json
cache.json
cache.gob.gz

# Debian package
btblocklist
config.json
debian/files
debian/btblocklist/
debian/btblocklist.substvars
debian/btblocklist.debhelper.log
debian/btblocklist.*.debhelper
debian/debhelper-build-stamp
